all:
  children:
    UAT11:
      children:
        App_Servers:
          hosts:
            app_server_01:
              ansible_ssh_host: uat11vsbl11.dhsie.hawaii.gov
              ansible_connection: ssh
        Web_Servers:
          hosts:
            web_server_01:
              ansible_ssh_host: uat11vsai11.dhsie.hawaii.gov
              ansible_connection: ssh